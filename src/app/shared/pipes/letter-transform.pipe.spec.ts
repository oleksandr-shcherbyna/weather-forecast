import { LetterTransformPipe } from './letter-transform.pipe';

describe('LetterTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new LetterTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
